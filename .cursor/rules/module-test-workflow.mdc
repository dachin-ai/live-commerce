---
description: 模块测试必须遵循「后端→MCP前端测试→有问题则修后端→无问题则出报告→人工终测」闭环
alwaysApply: true
---

# 模块测试流程（前后端闭环）

进行**按模块的测试、调试、验收**时，必须严格按以下顺序执行，不得跳过或调换。

## 1. 按模块实现/确认后端功能代码

- 先完成或确认**当前模块**对应的后端接口与逻辑（如 API、数据库、业务规则）。
- 范围仅限本模块，不顺手改其他模块。

## 2. 调用 MCP 测试前端，并对标市场成品方案

- 使用 **MCP 浏览器工具**（如 cursor-browser-extension）对前端进行自动化测试。
- 测试用例需**对标当前模块的市场成熟方案**（见 `docs/各模块步骤总结.md` 中各模块的「对标样本」）。
- 按该模块的测试步骤逐项执行，记录通过/失败。

## 3. 判断是否存在问题

- 若 MCP 测试或对标结果有**失败项、不符合预期、或与市场方案不一致** → 视为**存在问题**。

## 4. 存在问题 → 回到步骤 1

- **是**：回到「按模块实现后端功能代码」（修后端或前端与当前模块相关的代码），修完后再从步骤 2 重新用 MCP 测前端。
- 循环直到「判断是否存在问题」为**否**。

## 5. 无问题 → 输出模块报告

- **否**：输出**当前模块的测试报告**（含测试步骤、结果、对标结论、可选截图或 MCP 记录）。
- 报告可写在 `docs/` 下对应模块文档或《各模块步骤总结》的该模块小节。

## 6. 人工终测

- 报告输出后，流程进入**人工终测**阶段，由人工做最终确认。

---

## 参考文档

- **术语与定义**（时效、数据周期、时长单位、货币等）：`docs/交互定义与模块测试流程约定.md`
- 模块划分与测试步骤：`docs/各模块步骤总结.md`
- 详细闭环说明与检查清单：`docs/模块测试流程-前后端闭环.md`
- 多轮调试循环：`docs/测试调试多轮循环优化.md`

## 约束

- 未完成步骤 2（MCP 测试并对标）前，不输出「模块通过」报告。
- 发现问题时，必须先修复再重测，不得在报告中写「通过」却留有未修复问题。
