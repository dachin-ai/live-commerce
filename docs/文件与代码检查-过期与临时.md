# 文件与代码检查：过期与临时

对当前项目整体做了一次扫描，重点标注**过期**与**临时**相关文件及文档中的过期表述。建议按「建议操作」逐项处理。

---

## 一、已归档 / 无需处理

| 项 | 说明 |
|----|------|
| ~~`backend/archive/ai.ts.backup`~~ | 归档前源文件已处理：该备份已删除，项目入口以 `ai-refactored.ts` 为准。✅ |
| `backend/dist/` | 构建产物，已在 `.gitignore`，不提交。✅ |
| `*.log` | 已在 `.gitignore`，运行时生成的 `generate-tasks.log`、`coze-debug.log` 不会入库。✅ |

---

## 二、建议清理或归档

### 1. 后端脚本中的「疑似临时 / 重复」文件

以下脚本**未**在 `package.json` 或 `backend/scripts/README.md` 中列为正式命令，多为 `.js` 或与现有 `.ts` 功能重叠，可视为**临时或过期**，建议归档或删除：

| 文件 | 说明 | 建议 |
|------|------|------|
| `backend/scripts/test-generate-tasks.js` | 与 `test-generate-tasks-api.ts` 功能重叠 | ✅ 已删除 |
| `backend/scripts/test-parse-excel.js` | 与 `test-parse-excel.ts` 重叠 | ✅ 已删除 |
| `backend/scripts/clean-duplicates.js` | 未在 README/package.json 中，疑似一次性 | ✅ 已移至 `backend/archive/scripts/` |
| `backend/scripts/clear-greenpet-tasks.js` | 针对单一店铺的临时清理 | ✅ 已移至 `backend/archive/scripts/` |
| `backend/scripts/query-tasks.js` | 查询任务，未在 README 中 | ✅ 已移至 `backend/archive/scripts/` |
| `backend/scripts/view-tasks.js` | 查看任务，未在 README 中 | ✅ 已移至 `backend/archive/scripts/` |
| `backend/scripts/test-new-logic.js` | 疑似某次「新逻辑」测试用 | ✅ 已移至 `backend/archive/scripts/` |

**保留的脚本**（在 README 或文档中有说明或为正式命令）：  
`inspect-db.ts`、`clean-stats-duplicates.ts`、`clean-monthly-orphan-stats.ts`、`clear-import-data.ts`、`reset-database.ts`、`diagnose-excel.ts`、`test-parse-excel.ts`、`test-dashboard-apis.ts`、`test-generate-tasks-api.ts`、`trigger-workflow-round.ts`、`check-llm-url.ts`、`import-test-file.ts`；`clean-duplicate-tasks.sql` 若仍在使用可保留。

### 2. 数据库备份文件

| 文件 | 说明 | 建议 |
|------|------|------|
| `backend/data.db.backup` | 数据库的手动备份，可能为一次性 | 若不需要长期保留在仓库：加入 `.gitignore`（如 `backend/data.db.backup`），或移至项目外/备份目录并注明；若需保留在仓库，可在 `docs/启动与数据库操作指南.md` 中写明用途与恢复方式 |

---

## 三、文档中的过期表述（建议更新）

| 文档 | 位置 | 当前表述 | 建议修改 | 状态 |
|------|------|----------|----------|------|
| `docs/归档/历史优化与改进/今日对话回顾与规则恢复说明.md` | 第 6 行 | 入口与备份说明 | 已为「项目入口为 ai-refactored.ts，归档前备份已删除」 | ✅ 已一致 |
| `docs/系统层面分析与规划建议.md` | 冲突 2.1、五、2、程序工程师 | 备份文件 | 已改为「已删除」等 | ✅ 已处理 |

**说明**：历史报告类文档（如待办生成逻辑-数据化改造报告、模块5-6综合测试报告等）中仍有多处写 `backend/src/routes/ai.ts`，属**历史记录**，可不改；当前入口以 `项目结构说明.md`、`各模块步骤总结.md` 为准（`ai-refactored.ts`）。

---

## 四、Agent 移除后清理状态（2026-02）

| 项 | 说明 |
|----|------|
| 路由与实现 | `/api/agent` 及 `backend/src/routes/agent.ts` 已删除；`backend/.env.example` 已移除 `AGENT_API_KEY`。 |
| 脚本 | `backend/scripts/scheme-c-prompt-to-agent.ts` 已删除（曾请求已移除的 Agent 接口）。 |
| 工具函数 | `backend/src/utils/schemeCConverter.ts` 保留，当前**未被任何代码引用**，仅作「prompt→方案C结构化」工具保留供将来复用。 |
| 本地 .env | 若 `backend/.env` 中仍有 `AGENT_API_KEY=...`，可自行删除（.env 不提交，不影响他人）。 |
| 文档 | Agent 相关文档已废除并清除内容，仅保留重定向；见《LLM与智能生成-文档索引》第四节。当前约定见《Coze对接说明-待办与话术入参出参》。 |

---

## 五、非临时、需保留的文件（说明）

| 项 | 说明 |
|----|------|
| ~~`backend/src/routes/agent.ts`~~ | **已移除**（2026-02）。原第三方 Agent 协议入口；Coze 已迭代，不再通过本系统 Agent 通道。 |
| `frontend/src/components/AIFeatures.tsx` 中的 `SCRIPT_FORM_STORAGE_KEY = 'lvbcsym_script_form_draft'` | 「draft」为表单草稿持久化 key，属正常功能，非临时文件。 |
| `backend/src/routes/dataImport.ts` 中「删除临时文件」「清理临时文件」 | 指上传处理过程中的临时文件，属正常逻辑。 |
| `docs/模块3-6-测试与调试记录.md` 中「临时方案」 | 文档内对某方案的描述，非文件本身临时。 |
| 根目录 `index.html`、`test-terminal.html` | `.gitignore` 中注明保留为测试入口，保留。 |

---

## 六、建议操作汇总

1. **后端脚本**：✅ 已处理。与 .ts 重叠的 `test-generate-tasks.js`、`test-parse-excel.js` 已删除；有疑惑的 5 个已移至 `backend/archive/scripts/`；`scheme-c-prompt-to-agent.ts` 已删除（Agent 通路已移除）。详见该目录下 README。
2. **数据库备份**：在 `.gitignore` 中增加 `backend/data.db.backup`（若不想把备份提交到仓库）；或在文档中写明该文件用途与恢复方式。
3. **文档**：`今日对话回顾`、`系统层面分析与规划建议` 中关于 `ai.ts.backup` 的表述已与现状一致或已处理。
4. **Agent 移除**：✅ 已处理。路由、脚本、.env.example 与相关文档已清理/标注，见第四节。

---

## 七、未发现项

- 未发现 `.bak`、`.tmp`、`.temp`、`.old` 等临时扩展名文件（除 node_modules 内第三方库）。
- `backend/archive/` 现仅含 `scripts/`（5 个归档脚本）；`ai.ts.backup` 已删除。结构见 `backend/archive/scripts/README.md`。

---

*检查日期：2026-02-06；可根据后续变更定期刷新本表。*
