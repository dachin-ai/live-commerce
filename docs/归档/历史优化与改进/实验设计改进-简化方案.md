# 实验设计改进 - 简化方案

## 📅 方案时间
2026年2月2日

## 🎯 问题回顾

从实验设计角度发现的主要问题：

1. ❌ **平台数据源不匹配** - TikTok显示"抖音/快手"
2. ❌ **单场/主播任务重复生成** - 缺乏可控性
3. ❌ **异常检测阈值硬编码** - 缺乏数据支撑
4. ❌ **"完善店铺信息"判断过严** - 已填部分仍提示

---

## ✅ 已实施的修改

### 修改1：平台数据源适配 ✅

**位置**: `backend/src/routes/ai.ts` - `getConversionRateBenchmark()` 函数

**原代码**:
```typescript
const comparison = `低于「${categoryName}」品类基准 ${finalBenchmark}%${priceNote}（数据来源：抖音/快手平台2024-2025年Q4大盘数据）`
```

**修改方式**: 添加platform参数并动态生成数据源描述

**状态**: ✅ 已实施（待重启验证）

---

### 修改2：完善店铺信息判断优化 ✅

**位置**: `backend/src/routes/ai.ts` - `generateStageBasedTasks()` 函数

**原逻辑**:
```typescript
// 只要有一个为空就提示
if (!storeInfo.targetAudience || !storeInfo.brandPositioning) { ... }
```

**新逻辑**:
```typescript
// 缺失2个或以上才提示
const missingInfoCount = [
  !storeInfo.targetAudience,
  !storeInfo.brandPositioning,
  !storeInfo.minPrice || !storeInfo.maxPrice,
].filter(Boolean).length

if (missingInfoCount >= 2) { ... }
```

**状态**: ✅ 已实施

---

### 修改3：任务显示店铺名称 ✅

**位置**: 
- `backend/src/routes/tasks.ts` - GET / 路由
- `frontend/src/components/TaskList.tsx` - 任务渲染

**修改内容**:
- 后端：LEFT JOIN stores表获取店铺名称
- 前端：显示蓝色标签

**状态**: ✅ 已实施

---

## ⏳ 待实施的高优先级改进

由于代码结构复杂，为避免语法错误，建议分阶段实施：

### 阶段1：修复当前代码 ✅
1. ✅ 恢复备份文件
2. ✅ 仅应用3个核心修改（平台、判断、显示）
3. ⏳ 确保代码可运行

### 阶段2：单场和主播任务（建议单独实施）
**实施方式**: 创建新的辅助函数，不修改现有复杂逻辑

**伪代码**:
```typescript
// 在/generate-tasks路由的最后，添加单场和主播任务
router.post('/generate-tasks', async (req: AuthRequest, res) => {
  // ... 现有逻辑 ...
  
  // 【最后添加】单场和主播任务
  if (currentStats) {
    // 单场复盘
    const hasReviewTask = await dbGet('SELECT * FROM tasks WHERE title = "复盘最近一场直播" AND status = "pending" AND storeId = ?', [storeId])
    if (!hasReviewTask) {
      tasks.push({ title: '复盘最近一场直播', ... })
    }
    
    // 主播话术
    const hasScriptTask = await dbGet('SELECT * FROM tasks WHERE title = "优化主播话术脚本" AND status = "pending" AND storeId = ?', [storeId])
    if (!hasScriptTask) {
      tasks.push({ title: '优化主播话术脚本', ... })
    }
  }
  
  // ... 去重和创建逻辑 ...
})
```

### 阶段3：异常检测动态化（建议单独实施）
**实施方式**: 先在测试脚本中验证3σ算法，再集成到主代码

---

## 🚀 当前行动建议

### 方案A：保守方案（推荐） ✅
1. ✅ 仅实施已完成的3个修改（平台、判断、显示）
2. ⏳ 确保代码可运行
3. ⏳ 用户验证基本功能
4. ⏳ 后续再逐步添加单场和主播任务

### 方案B：完整方案
1. ⏳ 调试并修复当前代码的语法错误
2. ⏳ 完整实施所有P0改进
3. ⏳ 全面测试验证

---

## 📝 当前状态

### 已完成修改（3个）
1. ✅ 平台数据源适配（TikTok vs 抖音）
2. ✅ "完善店铺信息"判断优化（缺失2个才提示）
3. ✅ 任务显示店铺名称（蓝色标签）

### 待修复问题（1个）
1. ⚠️ 代码语法错误（第1740行附近）
   - 可能是括号未闭合
   - 可能是导出语句重复

### 建议下一步
1. 曾使用备份文件（ai.ts.backup，该备份已删除）
2. 手动应用3个核心修改
3. 确保代码可运行
4. 单场/主播任务后续单独实施

---

**当前文件状态**: ⚠️ 存在语法错误  
**建议方案**: 方案A（保守方案）  
**优先级**: 先确保基本功能可用
